# 變更紀錄

本文件記錄待辦清單 App 的功能變更。

## [0.2.0] - Step 2

### 新增

- 新增深色模式切換按鈕，可在淺色與深色主題之間切換。
- 新增主題切換按鈕圖示與文字提示：
  - 淺色模式時顯示「🌙 深色模式」
  - 深色模式時顯示「☀️ 淺色模式」
- 新增主題偏好儲存功能，使用者手動切換後會記錄至 localStorage，重新整理後可維持設定。
- 新增系統主題跟隨機制：若使用者尚未手動切換過主題，會依 `prefers-color-scheme` 自動套用。
- 新增待辦篩選功能，提供「全部 / 未完成 / 已完成」三種檢視。
- 新增篩選按鈕選中狀態樣式，清楚顯示目前套用的篩選條件。
- 新增篩選情境下的空清單提示文字，依目前篩選條件顯示對應訊息。

### 變更

- 調整標題列版面，於右側加入主題切換按鈕。
- 介面樣式延伸既有 CSS 變數機制，深色模式改以變數覆寫實作，避免在元件中寫死色碼。
- 清單渲染流程改為先依目前篩選條件計算可見項目再顯示。
- 底部「未完成：N 項」計算邏輯維持以整體待辦資料為準，不受篩選條件影響。

## [0.1.0] - Step 1

### 新增

- 新增待辦事項輸入框與「新增」按鈕，可建立新的待辦項目。
- 新增每筆待辦的完成勾選框，勾選後文字會加上刪除線並淡化顯示。
- 新增每筆待辦的刪除按鈕，可移除單一待辦項目。
- 新增底部統計資訊，顯示「未完成：N 項」。
- 新增空清單提示文字：「還沒有任何待辦事項,新增一個吧!」。
- 新增 localStorage 持久化保存，重新整理頁面後可保留待辦資料。
- 新增空白輸入防呆，避免新增空白內容。
- 新增卡片式置中版面、圓角、陰影與手機 RWD 支援。

### 變更

- 初版建立，無前一版本可比較。
